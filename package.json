{
  "name": "nicerquiz",
  "version": "0.1.0",
  "private": true,
  "description": "Quiz show da salotto: regia, tabellone e buzzer sul telefono.",
  "scripts": {
    "installa": "npm --prefix server install && npm --prefix client install",
    "server": "npm --prefix server run dev",
    "client": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "start": "npm --prefix client run build && npm --prefix server start",
    "prova": "npm --prefix server run prova"
  }
}
